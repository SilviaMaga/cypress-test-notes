const locNotes = {
    myNotes: '[data-testid="home"]', // Seletor para MyNotes
    categoryAll: '[data-testid="category-all"] > span', // Seletor para categoria ALL
    progressInfo: '[data-testid="progress-info"]', // Seletor para a informação de progresso
};

export default locNotes;